command:
  build: go build -gcflags="all=-N -l" -o ./tmp/tui.run ./cmd/burrow
  exec: ./tmp/tui.run
watch:
  folders:
    - "./cmd/burrow/"
    - "./internal/tui/"
    - "./internal/domain/"
    - "./internal/database/"
    - "./internal/config/"
