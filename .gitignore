# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test
coverage.out

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file (legacy - replaced by YAML config)
.env

# Database files (project-local - now uses XDG by default)
*.db
*.sqlite3
*.sqlite

# SQLC files
internal/database/queries.go

# logs (now uses XDG state directory)
*.logs

# Build artifacts (now uses XDG cache for servers)
burrow.bin

# Editor/IDE
# .idea/
# .vscode/

# Temporary files (legacy - replaced by XDG paths)
tmp
# Legacy files (now use XDG paths)
burrow.db-shm
burrow.db-wal

# Distribution files
DISTRIBUTION*
app.yaml
Makefile
distribution*
# Build artifacts (server binaries now use XDG cache)
bin/burrow-server
# CLI executable
cli

# XDG user data directories (should not be committed)
.cache/burrow/
.config/burrow/
.local/share/burrow/
.local/state/burrow/

Makefile

app.yaml

ARCHITECTURE.md

burrow
